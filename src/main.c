#include "_config.h"
#include "escalonador.h"
#include "fila.h"


int main() {
    
    filaInit(&alta);
    filaInit(&baixa);
    filaInit(&disco);
    filaInit(&fita);
    filaInit(&impressora);


    
    return 0; 
}