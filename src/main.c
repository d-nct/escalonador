#include "_config.h"
#include "escalonador.h"
#include "fila.h"


int main() {
    Fila alta;
    Fila baixa;
    Fila disco;
    Fila fita;
    Fila impressora;
    filaInit(&alta);
    filaInit(&baixa);
    filaInit(&disco);
    filaInit(&fita);
    filaInit(&impressora);


    
    return 0; 
}